const chai = require('chai');
const expect = chai.expect;
const should = chai.should();


describe('Number Array', () => {
  it('should have more than 2 numbers', function() {
    expect(start).to.have.length.above(2);
  });
});

